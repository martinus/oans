/*
 * Graceful shutdown (#201).
 *
 * A translation unit of the oans unit suite. Rebuilding this rebuilds only
 * these subjects, which is what makes a mutation sweep affordable.
 */
#include "suite.h"


#include "interrupt.c"

#include "test_interrupt.c"
